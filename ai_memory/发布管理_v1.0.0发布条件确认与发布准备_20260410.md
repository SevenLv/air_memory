# 发布管理_v1.0.0发布条件确认与发布准备_20260410

标签：发布, v1.0.0, GitHub Release, 里程碑

## 发布条件确认

**结论：满足发布条件**

依据 `doc/m6_report_v1.0.md` v1.1（验收结论"通过"）：

- M6 系统确认验收通过；
- SRD v1.0 全部 30 条需求验证覆盖；
- 后端 108 个单元测试、前端 77 个单元测试全部通过；
- 发现 4 条缺陷（DEF-001~DEF-004），已全部修复：
  - DEF-001 (Major)：mcp/server.py tier='cold' 修正为 'hot'；
  - DEF-002 (Critical)：docker-compose.yml INITIAL_VALUE_SCORE 从 0.5 修正为 0.6；
  - DEF-003 (Minor)：user_guide.md MCP save_memory 返回值示例修正；
  - DEF-004 (Minor)：user_guide.md HTTP 状态码描述 200 修正为 201。

## 发布材料

已准备以下发布材料：

| 文件 | 说明 |
| --- | --- |
| `doc/release_notes_v1.0.md` | v1.0.0 发布说明 |
| `doc/deploy_guide.md` | 部署手册（v1.0） |
| `doc/user_guide.md` | 用户手册（v1.0） |
| `.github/workflows/release.yml` | GitHub Actions 发布工作流 |

可部署程序：`start.sh`（macOS）、`start.bat`（Windows）、`docker-compose.yml`（随源码打包）。

## 发布操作步骤

发布工作流已创建（`.github/workflows/release.yml`），项目经理需执行以下步骤完成发布：

1. 合并当前 PR（copilot/check-release-criteria → main）；
2. 在 GitHub 仓库页面，进入 Actions → 发布 AIR_Memory；
3. 点击 "Run workflow"，输入版本号 `v1.0.0`，点击运行；
4. 工作流将自动创建 tag v1.0.0 并发布 GitHub Release，包含部署手册和用户手册。
