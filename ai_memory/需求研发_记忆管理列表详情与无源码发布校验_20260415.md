# 需求研发_记忆管理列表详情与无源码发布校验_20260415

标签：记忆管理, UI, 分页, 详情页, 发布流程, 无源码发布, API, 测试

## 最短路径

1. 先读取 `ai_rules/README.md` 与全部规则，并读取 `doc/tbp_v1.0.md`。
2. 与 Lydia 协作确认最小方案：后端补列表/详情接口，前端改 `/memories` 为默认最近列表并新增详情页。
3. 后端新增：
   - `GET /api/v1/memories/manage/list`（默认分页 20，支持 `memory_id`、`start_time`、`end_time`）；
   - `GET /api/v1/memories/{memory_id}/detail`（返回完整详情字段）。
4. 前端新增：
   - 记忆管理页默认加载最近记录、分页、ID/时间段筛选；
   - 列表项点击跳转 `/memories/:memoryId`；
   - 新增详情页展示 ID、原始数据、提交时间、价值评分及完整字段。
5. 发布流程增强：在 `.github/workflows/release.yml` 增加前端源码文件检查（`.ts/.vue`）并保持后端 `.py` 为空桩校验。
6. 文档更新：`doc/user_guide.md` 与 `doc/deploy_guide.md` 同步新 UI 和无源码发布策略说明。
7. 验证：
   - 后端：`python -m pytest backend/tests -q` 通过（155 passed, 1 skipped）；
   - 前端：`npm run build && npm test` 通过（9 files, 78 tests）。

## 备注

- `parallel_validation` 两次执行均因 Git diff 超时失败，需后续重试。
