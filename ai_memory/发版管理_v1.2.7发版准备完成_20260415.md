# 发版管理 - v1.2.7 发版准备完成

## 标签

发版, v1.2.7, 发版准备, release.yml, 版本号更新, 启动脚本优化, API charset 文档

## 任务背景

项目经理要求在 v1.2.6 基础上准备发版，目标版本为 v1.2.7。

## v1.2.6 之后变更梳理

v1.2.6 发布后，本分支包含以下改进（PR #56、PR #58）：

### PR #56 - 发布流程改进与文档更新

- `.github/workflows/release.yml`：新增 Python 字节码预编译和 zip 发布包构建步骤
- `doc/deploy_guide.md` v1.3：更新获取方式为从 GitHub Releases 下载发布包
- `README.md`：新增"获取发布包"章节，修正版本号
- `start.sh` / `start.bat`：移除冗余的 `pip install -e backend/` 命令

### PR #58 - REST API charset 文档补充

- `backend/src/air_memory/main.py`：FastAPI description 新增 charset=UTF-8 约束说明
- `backend/src/air_memory/api/memory.py`：POST 接口 docstring 补充 charset 约束
- `backend/tests/test_main.py`：新增 OpenAPI charset 文档验证测试

## 发版准备核查结果

| 项目 | 状态 | 备注 |
| --- | --- | --- |
| 版本号全栈更新至 1.2.7 | 完成 | main.py、pyproject.toml、package.json、start.bat、start.sh、README.md |
| 测试全部通过 | 完成 | 149 passed, 1 skipped |
| 发版说明文档 | 完成 | doc/release_notes_v1.2.7.md |
| 发版工作流更新 | 完成 | .github/workflows/release.yml 指向 release_notes_v1.2.7.md |
| frontend/package.json 版本对齐 | 完成 | 1.2.5 -> 1.2.7（跳过 1.2.6，与 backend 对齐） |

## 发版方式

合并本 PR 后，在 GitHub 仓库 Actions 页面手动触发"发布 AIR_Memory"工作流，输入 tag `v1.2.7` 即可创建 Release，自动生成 zip 发布包。

## 最短路径

1. 确认 v1.2.6 之后的 diff：`git diff v1.2.6 HEAD --name-only`
2. 更新版本号：main.py、pyproject.toml、package.json（text edit）、start.sh（text edit）、start.bat（python bytes replace）、README.md（text edit）
3. 更新 test_main.py 中版本号相关测试函数名和断言值
4. 创建 doc/release_notes_v{version}.md
5. 更新 .github/workflows/release.yml 的 `--notes-file` 引用
6. 运行测试确认通过
7. 提交 PR，合并后手动触发 release.yml
