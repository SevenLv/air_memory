# 发布管理 - 回归旧版发布方式

## 标签

发布, release.yml, 发布包, 源码包, start.sh, start.bat, v1.2.7

## 规则引用

- `/ai_rules/README.md` (v0.1)
- `/ai_rules/task_rules/basic_rules.md` (v0.1)
- `/ai_rules/memory_rules/basic_rules.md` (v0.1)
- `/ai_rules/file_rules/markdown_rules.md` (v0.1)

## 简明规则(面向本任务)

1. 先初始化子模块并读取全部规则, 再执行任务.
2. 先读取 `doc/tbp_v1.0.md`, 明确 Nia 负责文档和发布相关协作.
3. 改动前先跑现有测试/构建确认基线, 改动后再复测.
4. 任务结束后在 `/ai_memory` 记录可复用最短路径.

## 本次最短路径

1. 读取发布相关记忆和历史提交, 确认"之前发布方式"是 `39e7642` 对应方案.
2. 回退 `.github/workflows/release.yml` 的字节码预编译和空桩 `.py` 打包逻辑, 恢复 `backend/pyproject.toml + backend/src` 源码打包.
3. 恢复 `start.sh` 和 `start.bat` 中 `pip install --no-deps -e backend/` 行, 与源码包发布方式保持一致.
4. 同步 `doc/release_notes_v1.2.7.md` 的发布流程描述, 去掉字节码预编译表述.
5. 复测: backend `pytest -q` 通过, frontend `npm run test` 通过.
